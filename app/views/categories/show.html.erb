<p id="notice"><%= notice %></p>

<h3> Post under this category </h3>
<% Post.where(category_id: @category.id).each do |post| %>
<li> <%= post.title %> </li>
<% end %>


<%= link_to 'Edit', edit_category_path(@category) %> |
<%= link_to 'Back', categories_path %>
